# Archive

These HTML files are historical snapshots kept only for reference and rollback.

- `gamevault_html_backup.html`: earlier working copy before `index.html` became the canonical app file
- `gamevault_html_backup.pre-arcade.html`: pre-arcade redesign snapshot

For normal app changes, edit `../index.html`.
